import { ModuleWithProviders } from "@angular/core";
import { Icons } from "./icons.provider";
export declare class BootstrapIconsModule {
    private icons;
    constructor(icons: Icons);
    static pick(icons: {
        [key: string]: string;
    }): ModuleWithProviders<BootstrapIconsModule>;
}
